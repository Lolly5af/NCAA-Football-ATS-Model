# Backtest status

**Status: pending first real historical run.**

No ATS percentage is hard-coded or fabricated. The first genuine result will be generated by GitHub Actions after `CFBD_API_KEY` is configured.

The report will include:

- out-of-sample ATS accuracy
- favorite/market baseline accuracy
- Brier score and log loss
- season-by-season results
- spread buckets
- confidence tiers
- game-level predictions

The validation is walk-forward: each week's predictions can use only games that occurred before that week. The model is never evaluated on games it trained on.
